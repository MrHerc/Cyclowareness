# Cyclowareness backend — core dependencies
fastapi>=0.115
uvicorn[standard]>=0.32
sqlalchemy>=2.0
pydantic>=2.9
pydantic-settings>=2.6
PyJWT>=2.9
passlib>=1.7.4
anthropic>=0.40
httpx>=0.27
python-multipart>=0.0.12

# --- Production queue (optional; enable with TASK_RUNNER=celery) ---
# celery>=5.4
# redis>=5.0

# --- Production database (optional; set DATABASE_URL=postgresql+psycopg://...) ---
# psycopg[binary]>=3.2
